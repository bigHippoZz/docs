import { Ref, ref } from "vue";
import { Query } from "./typings";

// export const useAsyncQuery = <D, P extends unknown[]>(
//   query: Query<D, P>,
//   options
// ) => {
// 	const data = ref<D>()
// 	const error = ref<Error>()
// 	const params = ref() as Ref<P>
// };
